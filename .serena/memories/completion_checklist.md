# Completion Checklist
- After editing a change, run `openspec validate <change>`.
- Keep the change scoped to the active contract or milestone instead of expanding adjacent roadmap work.
- If tasks are part of the change, update `tasks.md` checkboxes immediately as work is completed.
- For future implementation work, review code and follow-up changes against the existing contract instead of silently widening the contract in-place.